import { Document } from "mongoose";

export interface {{camelcase name false }} extends Document {
  // Add your typescript interface here
  /*
  title: string;
  description?: string;
  owner_id?: string;
  dueDate: Date;
  */
  created: Date;
  updated: Date;
}